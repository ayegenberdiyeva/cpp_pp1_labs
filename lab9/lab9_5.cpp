#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int n, num;
    cin >> n;

    vector<int> list;
    for (int = 0; i<n; i++){
        cin >> "+" >> num;
    }

    return 0;
}